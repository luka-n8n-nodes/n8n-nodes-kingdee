import { ResourceOptions } from '../../help/type/IResource';

/**
 * 采购增值税专用发票资源定义
 * 业务对象表单Id: IV_PURCHASEIC
 */
const resource: ResourceOptions = {
	name: '采购增值税专用发票',
	value: 'purchaseIcInvoice',
	description: '采购增值税专用发票管理 (IV_PURCHASEIC)',
	order: 501,
};

export default resource;

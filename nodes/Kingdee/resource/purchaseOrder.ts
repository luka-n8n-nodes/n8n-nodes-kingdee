import { ResourceOptions } from '../../help/type/IResource';

/**
 * 采购订单资源定义
 * 业务对象表单Id: PUR_PurchaseOrder
 */
const resource: ResourceOptions = {
	name: '采购订单',
	value: 'purchaseOrder',
	description: '采购订单管理 (PUR_PurchaseOrder)',
	order: 200,
};

export default resource;

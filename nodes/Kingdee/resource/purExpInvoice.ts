import { ResourceOptions } from '../../help/type/IResource';

/**
 * 进项费用普通发票资源定义
 * 业务对象表单Id: IV_PUREXPINV
 */
const resource: ResourceOptions = {
	name: '进项费用普通发票',
	value: 'purExpInvoice',
	description: '进项费用普通发票管理 (IV_PUREXPINV)',
	order: 502,
};

export default resource;

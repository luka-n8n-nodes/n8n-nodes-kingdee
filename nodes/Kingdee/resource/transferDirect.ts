import { ResourceOptions } from '../../help/type/IResource';

/**
 * 直接调拨单资源定义
 * 业务对象表单Id: STK_TransferDirect
 */
const resource: ResourceOptions = {
	name: '直接调拨单',
	value: 'transferDirect',
	description: '直接调拨单管理 (STK_TransferDirect)',
	order: 223,
};

export default resource;

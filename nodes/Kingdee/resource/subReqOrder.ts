import { ResourceOptions } from '../../help/type/IResource';

/**
 * 委外订单资源定义
 * 业务对象表单Id: SUB_SUBREQORDER
 */
const resource: ResourceOptions = {
	name: '委外订单',
	value: 'subReqOrder',
	description: '委外订单管理 (SUB_SUBREQORDER)',
	order: 300,
};

export default resource;

import { ResourceOptions } from '../../help/type/IResource';

/**
 * 采购普通发票资源定义
 * 业务对象表单Id: IV_PURCHASEOC
 */
const resource: ResourceOptions = {
	name: '采购普通发票',
	value: 'purchaseOcInvoice',
	description: '采购普通发票管理 (IV_PURCHASEOC)',
	order: 500,
};

export default resource;
